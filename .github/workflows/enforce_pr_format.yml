name: Enforce PR Format

on:
  pull_request:
    types:
      - opened
      - synchronize

jobs:
  enforce-pr-format:
    runs-on: ubuntu-latest

    steps:
      - name: Check PR Description
        id: pr-description
        uses: actions/github-script@v6
        with:
          github-token: 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
          script: |
            const prDescription = context.payload.pull_request.body;
            const ticketPattern = /^IAM-\d+\s.*$/;
            if (!ticketPattern.test(prDescription)) {
              core.setFailed('PR description does not reference an IAM or Jira ticket.');
            }
